<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo cuando el item está seleccionado (verde) -->
    <item android:drawable="@color/verde" android:state_checked="true"/>
    <!-- Fondo cuando el item NO está seleccionado (transparente) -->
    <item android:drawable="@android:color/transparent"/>
</selector>
